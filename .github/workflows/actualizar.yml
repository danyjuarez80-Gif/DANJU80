name: Actualizar Listas IPTV

on:
  push:
    paths:
      - 'dan88.txt'
      - 'actualizar_lista.py'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Ejecutar script de limpieza
        run: python actualizar_lista.py

      - name: Configurar usuario de git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Subir cambios al repositorio
        run: |
          git add DANJU_TV.m3u DANJU_MOVIES.m3u DANJU_SERIES.m3u
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-actualización de listas IPTV" && git push)
